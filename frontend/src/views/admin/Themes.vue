<script setup>
import {useThemeStore} from '../../stores/theme';import AdminPageHeader from '../../components/admin/AdminPageHeader.vue'
const theme=useThemeStore()
</script><template><AdminPageHeader title="主题" description="切换当前主题，也为第三方主题开发者保留注册入口。"><template #actions><button class="secondary-button pressable">安装主题</button></template></AdminPageHeader><div class="theme-grid"><article v-for="item in theme.themes" :key="item.id" class="theme surface"><div class="preview" :class="item.id"><div class="bar"></div><div class="hero"></div><div class="cards"><i></i><i></i></div></div><div class="info"><div><h3>{{item.name}}</h3><p>{{item.description}}</p><small>{{item.author}} · {{item.version}}</small></div><button v-if="theme.themeId!==item.id" class="secondary-button pressable" @click="theme.setTheme(item.id)">启用</button><span v-else class="active">当前主题</span></div></article></div><section class="surface dev"><span class="eyebrow">Theme API</span><h2>第三方主题不需要改页面源码。</h2><p>通过主题注册表提供元数据和 CSS Token。未来主题包/商店接入后，这里可以直接读取后端安装状态。</p></section></template>
<style scoped>.theme-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.theme{border-radius:20px;overflow:hidden}.preview{height:250px;padding:16px;background:#f0f1f3}.preview.midnight{background:#07131d}.bar{height:30px;border-radius:9px;background:rgba(127,127,127,.22)}.hero{height:90px;width:60%;border-radius:13px;background:rgba(127,127,127,.16);margin:26px 0 14px}.cards{display:grid;grid-template-columns:1fr 1fr;gap:8px}.cards i{height:55px;border-radius:10px;background:rgba(127,127,127,.14)}.info{padding:18px;display:flex;justify-content:space-between;align-items:end;gap:15px}.info h3{margin:0}.info p{color:var(--text-2);font-size:.88rem}.info small{color:var(--text-3)}.active{color:var(--success);font-size:.82rem;font-weight:700}.dev{margin-top:14px;padding:30px;border-radius:20px}.dev h2{font-size:2rem;letter-spacing:-.04em}.dev p{color:var(--text-2);line-height:1.7;max-width:760px}@media(max-width:760px){.theme-grid{grid-template-columns:1fr}}</style>
