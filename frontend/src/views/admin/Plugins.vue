<script setup>
import {ref} from 'vue';import AdminPageHeader from '../../components/admin/AdminPageHeader.vue'
const plugins=ref([{id:'comments',name:'Comments',desc:'文章评论与审核能力。',enabled:false},{id:'seo',name:'SEO Toolkit',desc:'SEO 元数据与站点地图。',enabled:true},{id:'analytics',name:'Analytics',desc:'统计扩展接口示例。',enabled:false}])
</script><template><AdminPageHeader title="插件" description="让额外能力进入 Curly，而不是不断膨胀核心。"><template #actions><button class="secondary-button pressable">安装插件</button></template></AdminPageHeader><div class="plugins"><article v-for="p in plugins" :key="p.id" class="plugin surface"><div class="icon">⌘</div><div class="copy"><h3>{{p.name}}</h3><p>{{p.desc}}</p><small>{{p.id}}</small></div><button :class="['toggle',{on:p.enabled}]" @click="p.enabled=!p.enabled" :aria-label="p.enabled?'停用插件':'启用插件'"><span></span></button></article></div><section class="surface dev"><b>Plugin API 已预留</b><p>插件可注册前台插槽、后台菜单、后台页面和设置项。动态安装仍需要后端插件生命周期 API。</p></section></template>
<style scoped>.plugins{display:grid;gap:11px}.plugin{min-height:96px;padding:15px;border-radius:18px;display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:14px}.icon{width:52px;height:52px;border-radius:15px;background:var(--accent-soft);display:grid;place-items:center;font-size:1.3rem}.copy h3{margin:0}.copy p{margin:4px 0;color:var(--text-2);font-size:.88rem}.copy small{color:var(--text-3)}.toggle{width:48px;height:28px;padding:3px;border:0;border-radius:999px;background:var(--border);cursor:pointer;transition:background .2s ease}.toggle span{display:block;width:22px;height:22px;border-radius:99px;background:var(--surface-solid);box-shadow:var(--shadow-sm);transition:transform .25s var(--ease-fluid)}.toggle.on{background:var(--success)}.toggle.on span{transform:translateX(20px)}.dev{margin-top:14px;padding:20px;border-radius:18px}.dev p{color:var(--text-2);line-height:1.65}</style>
