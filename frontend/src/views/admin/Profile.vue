<script setup>
import {useAuthStore} from '../../stores/auth';import AdminPageHeader from '../../components/admin/AdminPageHeader.vue'
const auth=useAuthStore()
</script><template><AdminPageHeader title="个人资料" description="管理当前账户的基础信息和安全入口。"/><section class="profile surface"><div class="avatar">{{auth.displayName.slice(0,1).toUpperCase()}}</div><div><h2>{{auth.displayName}}</h2><p>{{auth.user?.email||'暂未读取邮箱'}}</p></div></section><div class="cards"><section class="surface card"><h3>账户信息</h3><label>用户名<input :value="auth.displayName"></label><label>邮箱<input :value="auth.user?.email||''"></label><button class="secondary-button pressable">保存资料</button></section><section class="surface card"><h3>安全</h3><p>密码修改、登录设备和二次验证接口尚未实现。</p><button class="secondary-button pressable">修改密码</button></section></div></template>
<style scoped>.profile{padding:22px;border-radius:20px;display:flex;align-items:center;gap:16px}.avatar{width:72px;height:72px;border-radius:22px;background:var(--accent);color:var(--accent-text);display:grid;place-items:center;font-size:2rem;font-weight:800}.profile h2{margin:0}.profile p{margin:5px 0 0;color:var(--text-3)}.cards{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px}.card{padding:22px;border-radius:20px}.card h3{margin-top:0}.card p{color:var(--text-2);line-height:1.65}.card label{display:grid;gap:7px;margin-bottom:14px;color:var(--text-2);font-size:.88rem}.card input{min-height:43px;border:1px solid var(--border);border-radius:12px;background:var(--surface-soft);color:var(--text-1);padding:0 11px}@media(max-width:760px){.cards{grid-template-columns:1fr}}</style>
