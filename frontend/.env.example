# 浏览器统一请求当前前端地址的 /api/v1。
# Vite 开发服务器再把 /api 转发给 Go HTTPS 后端。
# 因此手机访问电脑的 5173 时也不需要把 API 地址改成手机无法识别的 localhost。
VITE_API_BASE_URL=/api/v1

# Curly Go 后端实际监听地址。你的 PORT=8080 + RunTLS 时就是下面这个地址。
VITE_BACKEND_TARGET=https://127.0.0.1:8080

VITE_SITE_NAME=Curly
VITE_SITE_TAGLINE=写作、分享与发现
